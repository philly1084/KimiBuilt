---
apiVersion: v1
kind: Namespace
metadata:
  name: agent-platform
---
apiVersion: v1
kind: Secret
metadata:
  name: kimibuilt-remote-runner
  namespace: agent-platform
type: Opaque
stringData:
  KIMIBUILT_BACKEND_URL: "https://kimibuilt.demoserver2.buzz"
  KIMIBUILT_REMOTE_RUNNER_TOKEN: "REPLACE_WITH_THE_BACKEND_RUNNER_TOKEN"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kimibuilt-remote-runner
  namespace: agent-platform
data:
  KIMIBUILT_RUNNER_ID: "agent-platform-remote-cli"
  KIMIBUILT_RUNNER_NAME: "Agent Platform Remote CLI"
  KIMIBUILT_RUNNER_CAPABILITIES: "inspect,build,deploy"
  KIMIBUILT_RUNNER_ALLOWED_ROOTS: "/app,/workspace,/opt,/srv,/var/www,/tmp"
  KIMIBUILT_RUNNER_HEARTBEAT_MS: "15000"
  KIMIBUILT_RUNNER_MAX_OUTPUT_CHARS: "120000"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kimibuilt-remote-runner-kubeconfig
  namespace: agent-platform
data:
  k3s.yaml: |
    apiVersion: v1
    kind: Config
    clusters:
      - name: in-cluster
        cluster:
          certificate-authority: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          server: https://kubernetes.default.svc
    users:
      - name: runner
        user:
          tokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    contexts:
      - name: in-cluster
        context:
          cluster: in-cluster
          user: runner
    current-context: in-cluster
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimibuilt-remote-runner-workspace
  namespace: agent-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kimibuilt-remote-runner
  namespace: agent-platform
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kimibuilt-remote-runner
rules:
  - apiGroups: [""]
    resources: ["namespaces", "nodes", "pods", "pods/log", "services", "endpoints", "events", "configmaps"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["cert-manager.io"]
    resources: ["certificates", "challenges", "orders", "clusterissuers", "issuers"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["namespaces", "services", "configmaps"]
    verbs: ["create", "update", "patch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["create", "update", "patch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kimibuilt-remote-runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kimibuilt-remote-runner
subjects:
  - kind: ServiceAccount
    name: kimibuilt-remote-runner
    namespace: agent-platform
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kimibuilt-remote-runner
  namespace: agent-platform
  labels:
    app: kimibuilt-remote-runner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kimibuilt-remote-runner
  template:
    metadata:
      labels:
        app: kimibuilt-remote-runner
    spec:
      serviceAccountName: kimibuilt-remote-runner
      nodeSelector:
        kubernetes.io/arch: arm64
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: runner
          image: ghcr.io/philly1084/kimibuilt:latest
          imagePullPolicy: Always
          command:
            - /bin/sh
            - -ec
            - |
              mkdir -p /tmp/tools
              if [ ! -x /tmp/tools/kubectl ]; then
                curl -fsSL -o /tmp/tools/kubectl https://dl.k8s.io/release/v1.30.4/bin/linux/arm64/kubectl
                chmod 0755 /tmp/tools/kubectl
              fi
              exec node /app/bin/kimibuilt-runner.js
          envFrom:
            - configMapRef:
                name: kimibuilt-remote-runner
            - secretRef:
                name: kimibuilt-remote-runner
          env:
            - name: KUBECONFIG
              value: /etc/rancher/k3s/k3s.yaml
            - name: PATH
              value: /tmp/tools:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: BUILDKIT_HOST
              valueFrom:
                secretKeyRef:
                  name: agent-platform-runtime
                  key: buildkit-host
                  optional: true
          volumeMounts:
            - name: kubeconfig
              mountPath: /etc/rancher/k3s
              readOnly: true
            - name: workspace
              mountPath: /workspace
            - name: host-opt
              mountPath: /opt
            - name: host-srv
              mountPath: /srv
            - name: host-var-www
              mountPath: /var/www
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: kubeconfig
          configMap:
            name: kimibuilt-remote-runner-kubeconfig
        - name: workspace
          persistentVolumeClaim:
            claimName: kimibuilt-remote-runner-workspace
        - name: host-opt
          hostPath:
            path: /opt
            type: DirectoryOrCreate
        - name: host-srv
          hostPath:
            path: /srv
            type: DirectoryOrCreate
        - name: host-var-www
          hostPath:
            path: /var/www
            type: DirectoryOrCreate
