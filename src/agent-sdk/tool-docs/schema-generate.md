# schema-generate

Purpose: generate database schema artifacts from entity definitions.

Outputs can include:
- SQL DDL
- ORM schema
- ER diagram

Use when:
- the user describes tables/entities and relationships
- the user wants schema scaffolding

Key params:
- `entities`
- `database`
- `orm`
- `options`
