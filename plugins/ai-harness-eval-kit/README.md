# AI Harness Eval Kit

Eval design, fixture validation, harness runs, and prompt/model regression reports.

Only the marketplace entry was present in the uploaded files. This placeholder manifest keeps the suite reference complete, but no skills or helper scripts were supplied.
