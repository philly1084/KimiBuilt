# AI Harness Maintainer Scripts

This directory is reserved for helper scripts used by the plugin.

Good future additions:

- A repository baseline collector that emits harness entrypoints, test commands, and UI targets.
- A recurring-run report normalizer.
- A screenshot artifact indexer for `ui-checks/` outputs.
