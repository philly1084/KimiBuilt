apiVersion: v1
kind: Service
metadata:
  name: sandbox-app
  namespace: __NAMESPACE__
spec:
  selector: {app.kubernetes.io/name: sandbox-app}
  ports:
    - name: http
      port: 80
      targetPort: http
